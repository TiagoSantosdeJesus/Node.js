/*Exercício:
Sem usar bibliotecas, crie um projeto simples em Node.js que seja capaz de responder requisições HTTP.
Explique como rodar e testar.*/

// Importação da biblioteca http
const http = require('http');
const app= http();

// Requisição web para que o servidor responda e imprima na tela quando estiver rodando.
app.get('/', (req, res) => {
    res.send("O servidor está ativo!");
});
servidor.listen(3000,() => {
    console.log("Servidor iniciado na porta 3000");
});
